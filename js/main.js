/**
 * Main — Entry point voor alle pagina's.
 * Initialiseert gedeelde componenten.
 * Wordt geladen met defer, dus DOM is beschikbaar.
 */

(function init() {
  initPageShell();
  initTheme();
  initNavigation();
})();
